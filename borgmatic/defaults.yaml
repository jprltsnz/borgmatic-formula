# -*- coding: utf-8 -*-
# vim: ft=yaml
---
borgmatic:
  pkg:
    name: borgmatic
  rootgroup: root
  config: '/etc/borgmatic'
  service:
    name: borgmatic-timer
  subcomponent:
    config: '/etc/borgmatic-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
